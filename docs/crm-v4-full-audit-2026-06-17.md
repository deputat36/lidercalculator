# CRM v4 — полный аудит и доработки от 17.06.2026

## Цель аудита

Проверить текущую CRM v4 для РА «Лидер» после серии доработок: карточка заявки, расчёты, КП, заказы, клиенты, редакторы сущностей, нагрузка на Supabase и структура подключенных модулей.

## Что было найдено и исправлено

### 1. Дублирование редактора заявки

В проекте одновременно работали два редактора заявки:

- старый встроенный `lead-edit.js`;
- новый универсальный редактор `entity-edit-v2.js`.

Это могло давать дублирующиеся кнопки, путаницу в карточке и лишний визуальный шум.

Что исправлено:

- `lead-edit.js` отключён из `app-v4.html`;
- вместо него используется новый универсальный редактор `entity-edit-v3.js`;
- `entity-edit-v2.js` больше не подключается к основной CRM v4;
- в `lead-card-layout-fix.js` оставлена защитная очистка старых элементов `editLeadBtn` и `leadEditBox`, если они остались в кэше браузера.

### 2. Карточка заявки была перегружена

Карточка заявки сразу раскрывала все рабочие блоки:

- сообщение клиента;
- потребности;
- сохранённые расчёты;
- типовой расчёт;
- нестандартный расчёт;
- КП;
- ссылки и источник;
- технические данные формы.

Это мешало быстро понять, что делать с заявкой.

Что исправлено:

- добавлен модуль `lead-card-accordion.js`;
- рабочие блоки карточки переведены в сворачивающиеся секции;
- открыты по умолчанию только основные блоки: сообщение клиента, потребности, сохранённые расчёты;
- типовой расчёт, нестандартный расчёт, КП, ссылки и технические данные по умолчанию свернуты.

### 3. Риск неправильного порядка блоков после аккордеона

После оборачивания блоков в `<details>` была вероятность, что скрипт перестановки перенесёт внутренний блок вместо визуального блока-аккордеона.

Что исправлено:

- `lead-card-layout-fix.js` теперь работает с визуальным блоком через `closest('details[data-lead-accordion="1"]')`;
- порядок блоков сохраняется безопасно:
  1. сохранённые расчёты;
  2. быстрый типовой расчёт;
  3. нестандартный расчёт;
  4. КП.

### 4. Старый калькулятор был убран из карточки

Ранее в карточке заявки визуально было два расчётных блока: старый калькулятор и новый быстрый/нестандартный расчёт.

Что исправлено:

- `calculations-catalog.js` больше не подключается к `app-v4.html`;
- вместо него используется `calculations-saved-tools-v2.js`, который показывает только сохранённые расчёты;
- создание новых расчётов вынесено в отдельные понятные блоки:
  - `calculations-standard.js` — типовые позиции;
  - `calculations-advanced.js` — нестандартные позиции.

### 5. Лишние фоновые запросы редактора

`entity-edit-v2.js` использовал постоянный `setInterval(enhance, 2200)`, из-за чего каждые 2,2 секунды могли запускаться проверки списков для добавления кнопок редактирования.

Что исправлено:

- создан `entity-edit-v3.js`;
- постоянный таймер убран;
- редактор обновляет кнопки только по событиям:
  - готовность CRM;
  - открытие карточки заявки;
  - смена маршрута;
  - клик по вкладке;
  - обновление списка.

### 6. Дублирующий редактор заказа

После перехода на `entity-edit-v3.js` отдельный модуль `entity-edit-order-fix.js` стал лишним: он тоже добавлял кнопку редактирования заказа и держал постоянный интервал.

Что исправлено:

- `entity-edit-order-fix.js` отключён из `app-v4.html`;
- редактирование заказа теперь полностью обслуживается `entity-edit-v3.js`.

### 7. Старый inline-скрипт вкладок

Внизу `app-v4.html` оставался старый inline-скрипт переключения вкладок. Он дублировал `main-menu-fix.js` и мог конфликтовать с обработчиками вкладок.

Что исправлено:

- inline-скрипт удалён из `app-v4.html`;
- совместимость `window.v4SetTab` перенесена в `main-menu-fix.js`;
- стартовое меню в HTML приведено к актуальным разделам: заявки, заказы, клиенты, расчёты, КП, номенклатура, настройки.

### 8. Дублирующиеся индексы в Supabase

Supabase Performance Advisor показал дубли индексов в таблицах CRM «Лидер».

Удалены безопасные дубли:

- `leader_lead_needs_lead_id_idx`;
- `leader_lead_needs_status_idx`;
- `idx_leader_leads_next_contact_at`;
- `idx_leader_leads_status_created_desc`.

Оставлены основные аналоги:

- `idx_leader_lead_needs_lead_id`;
- `idx_leader_lead_needs_status`;
- `idx_leader_leads_next_contact`;
- `idx_leader_leads_status_created`.

### 9. Дублирующая RLS-политика заявок

У `leader_leads` были две SELECT-политики для авторизованных пользователей:

- `leader_leads_select_active_users`;
- `leader_leads_select_app`.

`leader_has_access()` оказался тем же базовым условием активного пользователя, поэтому дубль был удалён.

Что исправлено:

- удалена политика `leader_leads_select_active_users`;
- оставлена политика `leader_leads_select_app` через `leader_has_access()`.

### 10. Карточка клиента

Раньше в разделе «Клиенты» можно было открыть только последнюю заявку или редактировать данные. Не было единого обзора по клиенту.

Что добавлено:

- создан модуль `client-card-v1.js`;
- в разделе «Клиенты» появляется кнопка `Карточка клиента`;
- карточка клиента показывает:
  - все найденные заявки клиента;
  - КП по этим заявкам;
  - заказы клиента;
  - сумму заказов;
  - сумму бюджетов заявок;
  - быстрые кнопки открытия и редактирования.

### 11. Карточка КП

Раньше в общем разделе КП и в карточке клиента не было полноценной карточки коммерческого предложения.

Что добавлено:

- создан модуль `offer-card-v1.js`;
- кнопка `Открыть КП` добавляется в общий список КП;
- кнопка `Открыть КП` добавляется в карточку заявки рядом с КП;
- кнопка `Открыть КП` добавляется в карточку клиента рядом с КП;
- карточка КП показывает:
  - полный текст КП;
  - короткий текст КП;
  - состав позиций из связанного расчёта;
  - связанный расчёт;
  - связанную заявку;
  - связанный заказ, если он есть;
  - историю изменения статусов КП;
  - быстрые кнопки открытия заявки, заказа, редактирования КП и копирования текста.

### 12. Supabase API-логи

Проверены API-логи Supabase. По CRM-запросам видны в основном ответы `200` и `201`. Критических ошибок уровня `500` по основному сценарию не обнаружено.

## Что осталось как технический долг

### 1. RLS performance warnings

Supabase показывает предупреждения по некоторым RLS-политикам, где функции `auth.*` могут вычисляться построчно. Это не авария, но на большой базе может замедлять запросы.

Нужно позже отдельно пройтись по политикам только `leader_*` и заменить вызовы на формат `(select auth.uid())` / `(select auth.email())`, где это безопасно.

### 2. SECURITY DEFINER warnings

Supabase показывает предупреждения по `SECURITY DEFINER` функциям. Для части `leader_*` это ожидаемо, потому что функции используются для CRM-операций. Но позже нужно отдельно пройтись по правам `EXECUTE` и оставить доступ только там, где он действительно нужен.

`nav_*` и `nav_v2_*` не трогались, потому что это другой проект.

### 3. Улучшение заказа из КП

Карточка КП уже добавлена. Следующий логичный этап — сделать более управляемое создание заказа из КП:

- кнопка `Создать заказ` прямо в карточке КП;
- выбор срока, предоплаты, статуса макета и производственного комментария перед созданием заказа;
- перенос состава расчёта в заказ без ручного копирования.

## Проверочный URL

Открывать с обновлением кэша:

`https://deputat36.github.io/lidercalculator/app-v4.html?offer-card=20260617`

## Итог

CRM стала компактнее и логичнее:

- карточка заявки больше не раскрывает всё сразу;
- старый редактор заявки отключён;
- универсальный редактор стал легче;
- лишний редактор заказа отключён;
- старый inline-скрипт вкладок удалён;
- старый калькулятор убран;
- расчёты разделены на сохранённые, типовые и нестандартные;
- добавлена карточка клиента;
- добавлена карточка КП;
- лишние дубли индексов в базе удалены;
- дублирующая SELECT-политика заявок удалена;
- критических Supabase API-ошибок по основным CRM-запросам не найдено.
