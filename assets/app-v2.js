const SUPABASE_URL = window.LEADER_SUPABASE_URL || 'https://ofewxuqfjhamgerwzull.supabase.co';
const SUPABASE_KEY = window.LEADER_SUPABASE_KEY || 'sb_publishable_ZiX8_Mnf0dY6S__tKO2A4A_uD94G2cs';
const db = window.db || window.supabase.createClient(SUPABASE_URL, SUPABASE_KEY, {
  auth: { persistSession: true, autoRefreshToken: true, detectSessionInUrl: false, storageKey: window.LEADER_SUPABASE_AUTH_STORAGE || 'leader_session_v1' }
});
window.db = db;
const state = { user:null, profile:null, leads:[], dashboardStats:null, leadsLoaded:false, activeLead:null, rows:[], authBusy:false, crmReady:false };
const statuses = ['Новая','В работе','Уточнение деталей','Расчёт подготовлен','КП отправлено','Ждём ответ','Нужно пересчитать','Согласовано','Создан заказ','Отказ','Не отвечает','Дорого','Передумал','Спам'];
const sources = ['MAX','ВК','Звонок','Авито','Сайт','Яндекс Карты','2ГИС','Рекомендация','Постоянный клиент','Другое'];
function $(id){return document.getElementById(id)}
function money(v){return Math.round(Number(v||0)).toLocaleString('ru-RU')+' ₽'}
function esc(s){return String(s??'').replace(/[&<>\"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[m]))}
function toast(text){const t=$('toast');if(!t)return;t.textContent=text;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),2400)}
function dt(v){try{return v?new Date(v).toLocaleString('ru-RU'):'—'}catch(e){return v||'—'}}
function val(id){return $(id)?.value?.trim()||''}
function num(id){return Number($(id)?.value||0)||0}
function sleep(ms){return new Promise(r=>setTimeout(r,ms))}
function withTimeout(promise,ms,message){return Promise.race([promise,new Promise((_,reject)=>setTimeout(()=>reject(new Error(message||'Превышено время ожидания')),ms))])}
function setAuth(text,ok=false){const e=$('authState');if(!e)return;e.textContent=text;e.className='auth-state '+(ok?'good':'warn')}
function setAuthBusy(on,text='Вхожу...'){state.authBusy=!!on;const loginBtn=$('loginBtn'),logoutBtn=$('logoutBtn'),resetBtn=$('resetAuthBtn');if(loginBtn){loginBtn.disabled=!!on;loginBtn.textContent=on?text:'Войти'}if(logoutBtn)logoutBtn.disabled=!!on;if(resetBtn)resetBtn.disabled=!!on}
function clearLeaderSession(){const patterns=['leader_session_v1','leader_','ofewxuqfjhamgerwzull-auth-token'];try{Object.keys(localStorage).forEach(k=>{if(patterns.some(p=>k.indexOf(p)>=0))localStorage.removeItem(k)})}catch(e){}try{Object.keys(sessionStorage).forEach(k=>{if(patterns.some(p=>k.indexOf(p)>=0))sessionStorage.removeItem(k)})}catch(e){}}
function ensureLeadCardStyles(){if($('leadCardDetailsCss'))return;const s=document.createElement('style');s.id='leadCardDetailsCss';s.textContent='.lead-details-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:8px;margin:10px 0}.lead-detail{background:#f9fafb;border:1px solid #e5e7eb;border-radius:12px;padding:8px 10px}.lead-detail span{display:block;font-size:11px;color:#667085;font-weight:800;margin-bottom:3px}.lead-detail b{font-size:13px;color:#111827}.lead-client-actions{display:flex;gap:8px;flex-wrap:wrap;margin-top:10px}.lead-client-actions a{display:inline-flex;align-items:center;justify-content:center;border:1px solid #e5e7eb;background:#fff;border-radius:999px;padding:8px 11px;font-size:13px;font-weight:900;text-decoration:none;color:#111827}.lead-message strong{font-weight:900}.lead-message-clean{white-space:pre-wrap}@media(max-width:900px){.lead-details-grid{grid-template-columns:repeat(2,minmax(0,1fr))}}@media(max-width:560px){.lead-details-grid{grid-template-columns:1fr}.lead-client-actions a{flex:1}}';document.head.appendChild(s)}
function clearDetailsFromMessage(msg){return String(msg||'').split('\n').filter(line=>!/^\s*(Город|Связь|Размеры|Количество|Срок|Макет|Доставка\/монтаж)\s*:/i.test(line)).join('\n').replace(/^Задача:\s*/i,'').trim()}
function lineValue(msg,label){const re=new RegExp('^\\s*'+label+'\\s*:\\s*(.+)$','im');const m=String(msg||'').match(re);return m?m[1].trim():''}
function leadDetails(lead){const p=lead.payload||{};const msg=lead.message||'';let size='';if(p.width||p.height)size=(p.width||'-')+'×'+(p.height||'-')+' м';else size=lineValue(msg,'Размеры');const details=[['Город',lead.city||p.city||lineValue(msg,'Город')],['Связь',p.contact_method||lineValue(msg,'Связь')],['Размер',size],['Количество',p.quantity||lineValue(msg,'Количество')],['Срок',p.deadline||lineValue(msg,'Срок')],['Макет',p.mockup||lineValue(msg,'Макет')],['Доставка/монтаж',p.delivery||lineValue(msg,'Доставка\\/монтаж')]];return details.filter(x=>x[1]&&String(x[1]).trim())}
function renderLeadDetails(lead){const d=leadDetails(lead);if(!d.length)return '';return '<div class="lead-details-grid">'+d.map(x=>`<div class="lead-detail"><span>${esc(x[0])}</span><b>${esc(x[1])}</b></div>`).join('')+'</div>'}
function contactLinks(lead){const phone=String(lead.phone||'').replace(/[^\d+]/g,'');const links=[];if(phone)links.push(`<a href="tel:${esc(phone)}">Позвонить</a>`);if(phone)links.push(`<a href="https://wa.me/${esc(phone.replace(/^8/,'7').replace(/^\+/,'').replace(/\D/g,''))}" target="_blank" rel="noopener">WhatsApp</a>`);return links.length?'<div class="lead-client-actions">'+links.join('')+'</div>':''}
async function getProfile(user){const {data,error}=await withTimeout(db.from('leader_user_profiles').select('email,role,is_active,full_name').eq('user_id',user.id).maybeSingle(),12000,'Профиль пользователя не загрузился за 12 секунд');if(error)return null;return data}
function applyAuthUi(session,profile){if(!session?.user){state.user=null;state.profile=null;state.crmReady=false;setAuth('Вход не выполнен. Введите email и пароль.');$('authForm')?.classList.remove('hidden');$('logoutBtn')?.classList.add('hidden');return false}state.user=session.user;state.profile=profile||state.profile||null;const role=state.profile?.role?' • роль: '+state.profile.role:'';setAuth('Вход выполнен: '+session.user.email+role,true);$('authForm')?.classList.add('hidden');$('logoutBtn')?.classList.remove('hidden');return true}
async function checkAuth(){try{setAuth('Проверяю вход...');const {data}=await withTimeout(db.auth.getSession(),12000,'Проверка входа не ответила за 12 секунд');const session=data.session;if(!session?.user)return applyAuthUi(null,null);const profile=await getProfile(session.user);return applyAuthUi(session,profile)}catch(e){const msg=String(e.message||e).toLowerCase();if(msg.includes('expired')||msg.includes('refresh')||msg.includes('jwt')){clearLeaderSession();state.user=null;state.profile=null;state.crmReady=false;$('authForm')?.classList.remove('hidden');$('logoutBtn')?.classList.add('hidden');setAuth('Старая сессия устарела. Нажмите «Войти» заново.');return false}setAuth('Ошибка проверки входа: '+e.message);$('authForm')?.classList.remove('hidden');$('logoutBtn')?.classList.add('hidden');return false}}
async function loadCrmData(){setAuth('Вход выполнен. Загружаю заявки...',true);await loadLeads();state.leadsLoaded=true;state.crmReady=true;const role=state.profile?.role?' • роль: '+state.profile.role:'';setAuth('CRM готова: '+(state.user?.email||'пользователь')+role,true)}
async function login(){if(state.authBusy)return;const email=val('loginEmail'),password=val('loginPassword');if(!email||!password)return alert('Введите email и пароль');setAuthBusy(true,'Вхожу...');setAuth('Выполняю вход...');try{clearLeaderSession();await sleep(100);const {data,error}=await withTimeout(db.auth.signInWithPassword({email,password}),25000,'Сервер входа не ответил за 25 секунд. Проверьте интернет и попробуйте ещё раз.');if(error){setAuth('Вход не выполнен: '+error.message);alert(error.message);return}const session=data?.session||null;if(!session?.user){setAuth('Вход не выполнен: сессия не получена');return}const profile=await getProfile(session.user);applyAuthUi(session,profile);await loadCrmData();toast('Вход выполнен, CRM готова')}catch(e){setAuth('Ошибка входа: '+e.message);alert(e.message)}finally{setAuthBusy(false)}}
async function logout(){if(state.authBusy)return;setAuthBusy(true,'Выхожу...');setAuth('Выполняю выход...');try{await withTimeout(db.auth.signOut(),10000,'Выход не завершился за 10 секунд')}catch(e){}clearLeaderSession();state.user=null;state.profile=null;state.crmReady=false;state.leads=[];state.activeLead=null;state.rows=[];applyAuthUi(null,null);renderDashboard();renderLeads();renderCalcRows();toast('Вы вышли из CRM');setAuthBusy(false)}
function ensureResetAuthButton(){const host=document.querySelector('.auth-card');if(!host||$('resetAuthBtn'))return;const btn=document.createElement('button');btn.id='resetAuthBtn';btn.type='button';btn.textContent='Сбросить вход';btn.title='Очистить устаревшую сессию CRM «Лидер»';btn.onclick=async()=>{if(state.authBusy)return;setAuthBusy(true,'Сбрасываю...');try{await db.auth.signOut()}catch(e){}clearLeaderSession();state.user=null;state.profile=null;state.crmReady=false;$('authForm')?.classList.remove('hidden');$('logoutBtn')?.classList.add('hidden');setAuth('Сессия очищена. Введите email и пароль заново.');renderDashboard();renderLeads();toast('Старая сессия очищена');setAuthBusy(false)};host.appendChild(btn)}
async function api(body){const ok=await checkAuth();if(!ok)throw new Error('Сначала войдите в CRM');const {data,error}=await db.functions.invoke('leader-crm-leads',{body});if(error)throw new Error(error.message||'Ошибка запроса');if(data?.error)throw new Error(data.error+(data.details?': '+data.details:''));return data||{}}
async function loadLeads(){const data=await api({action:'list'});state.leads=data.leads||[];fillSourceFilter();renderLeads();renderDashboard();return state.leads}
function fillSourceFilter(){const sel=$('leadSourceFilter');if(!sel)return;const old=sel.value||'Все';const set=new Set(['Все']);state.leads.forEach(l=>set.add(l.source||'Не указан'));sel.innerHTML=[...set].map(x=>`<option>${esc(x)}</option>`).join('');sel.value=[...set].includes(old)?old:'Все'}
function statusBadge(status){let cls='';if(['Создан заказ','Согласовано'].includes(status))cls='good';if(['Ждём ответ','КП отправлено','Уточнение деталей','Нужно пересчитать'].includes(status))cls='warn';if(['Отказ','Дорого','Передумал','Не отвечает'].includes(status))cls='bad';return `<span class="badge ${cls}">${esc(status||'Новая')}</span>`}
function filteredLeads(){const status=$('leadStatusFilter')?.value||'active';const source=$('leadSourceFilter')?.value||'Все';const q=val('leadSearch').toLowerCase();return state.leads.filter(l=>{const st=l.status||'Новая';if(status==='active'&&(st==='Спам'||st==='Создан заказ'||st==='Отказ'))return false;if(status!=='active'&&status!=='Все'&&st!==status)return false;if(source!=='Все'&&(l.source||'Не указан')!==source)return false;if(q){const hay=[l.name,l.phone,l.service,l.message,l.source,l.city,JSON.stringify(l.payload||{})].join(' ').toLowerCase();if(!hay.includes(q))return false}return true})}
function renderLeads(){ensureLeadCardStyles();const box=$('leadList');if(!box)return;const list=filteredLeads();if(!list.length){box.innerHTML='<div class="empty">Заявок по выбранным условиям нет.</div>';return}box.innerHTML=list.map(l=>{const clean=clearDetailsFromMessage(l.message||'');return `<article class="lead-card" data-id="${esc(l.id)}"><div><div class="lead-title">${esc(l.name||'Без имени')} ${statusBadge(l.status||'Новая')}</div><div class="meta">${dt(l.created_at)} • ${esc(l.source||'Источник не указан')} • ${esc(l.service||'Услуга не указана')}</div><div class="meta">Телефон: ${esc(l.phone||'—')}${l.budget?' • Бюджет: '+money(l.budget):''}</div>${renderLeadDetails(l)}${clean?`<div class="lead-message"><strong>Комментарий:</strong><br><span class="lead-message-clean">${esc(clean)}</span></div>`:''}${contactLinks(l)}</div><div class="lead-actions"><select data-action="status">${statuses.map(s=>`<option ${s===(l.status||'Новая')?'selected':''}>${s}</option>`).join('')}</select><button data-action="calc" class="primary">В расчёт</button><button data-action="client">Клиент</button><button data-action="spam">Спам</button></div></article>`}).join('')}
async function updateLead(id,patch){const data=await api({action:'update',id,...patch});const lead=data.lead;if(lead)state.leads=state.leads.map(l=>l.id===id?{...l,...lead}:l);renderLeads();renderDashboard()}
async function createManualLead(){const data=await api({action:'create',name:val('leadName'),phone:val('leadPhone'),source:val('leadSource'),service:val('leadService'),budget:val('leadBudget'),city:val('leadCity'),message:val('leadMessage')});if(data.lead)state.leads=[data.lead,...state.leads];closeLeadModal();renderLeads();renderDashboard();toast('Заявка создана')}
async function ensureClient(lead){const data=await api({action:'ensure_client',name:lead.name,phone:lead.phone,source:lead.source||'CRM',comment:[lead.service,lead.message].filter(Boolean).join('\n')});await updateLead(lead.id,{status:'В работе'});toast(data.existed?'Клиент уже был в базе':'Клиент создан')}
function leadToCalc(lead){state.activeLead=lead;$('calcClientName').value=lead.name||'';$('calcClientPhone').value=lead.phone||'';$('calcSource').value=sources.includes(lead.source)?lead.source:'Другое';const clean=clearDetailsFromMessage(lead.message||'');const details=leadDetails(lead).map(x=>x[0]+': '+x[1]).join('\n');$('calcComment').value=[lead.service,clean,details].filter(Boolean).join('\n');$('calcOrderType').value=guessOrderType(lead.service+' '+lead.message);showActiveLead();updateLead(lead.id,{status:'В работе'}).catch(console.warn);openPage('calc')}
function guessOrderType(text){const t=String(text||'').toLowerCase();const production=['баннер','наклей','таблич','плен','плён','печать','вывес','пвх','монтаж'];const service=['логотип','карточ','яндекс','2гис','текст','контент','ведение','соцсет','реклама','seo','фирмен'];const hasP=production.some(x=>t.includes(x)),hasS=service.some(x=>t.includes(x));if(hasP&&hasS)return'Смешанный';if(hasP)return'Изготовление';if(hasS)return'Услуга';return'Смешанный'}
function showActiveLead(){const box=$('activeLeadBox');if(!box)return;if(!state.activeLead){box.classList.add('hidden');box.innerHTML='';return}box.classList.remove('hidden');box.innerHTML=`<b>Расчёт связан с заявкой:</b> ${esc(state.activeLead.name||'Без имени')} • ${esc(state.activeLead.service||'Услуга не указана')} <button id="unlinkLeadBtn">Отвязать</button>`;$('unlinkLeadBtn').onclick=()=>{state.activeLead=null;showActiveLead()}}
function addItem(){const row={id:Date.now()+Math.random(),type:val('itemType'),name:val('itemName'),unit:val('itemUnit'),qty:num('itemQty')||1,price:num('itemCost'),client:num('itemClient'),comment:val('itemComment')};if(!row.name)return alert('Укажите название позиции');state.rows.push(row);['itemName','itemCost','itemClient','itemComment'].forEach(id=>$(id).value=id==='itemCost'||id==='itemClient'?'0':'');$('itemQty').value='1';renderCalcRows()}
function calcTotals(){const cost=state.rows.reduce((a,r)=>a+Number(r.price||0)*Number(r.qty||0),0);const total=state.rows.reduce((a,r)=>a+Number(r.client||0)*Number(r.qty||0),0);return{cost,total,profit:total-cost,balance:total}}
function renderCalcRows(){const tb=$('calcRows');if(!tb)return;tb.innerHTML=state.rows.map((r,i)=>`<tr><td>${esc(r.type)}</td><td>${esc(r.name)}</td><td>${esc(r.unit)}</td><td>${r.qty}</td><td>${money(Number(r.price||0)*Number(r.qty||0))}</td><td>${money(Number(r.client||0)*Number(r.qty||0))}</td><td>${esc(r.comment)}</td><td><button data-remove="${i}">×</button></td></tr>`).join('')||'<tr><td colspan="8">Позиции ещё не добавлены.</td></tr>';const t=calcTotals();$('totalCost').textContent=money(t.cost);$('totalClient').textContent=money(t.total);$('totalProfit').textContent=money(t.profit)}
async function markQuoteSent(){if(!state.activeLead)return alert('Расчёт не связан с заявкой. Сначала выберите заявку и нажмите «В расчёт».');await updateLead(state.activeLead.id,{status:'КП отправлено',estimated_amount:calcTotals().total});state.activeLead.status='КП отправлено';toast('Статус заявки: КП отправлено')}
async function createOrder(){if(!state.rows.length)return alert('Добавьте хотя бы одну позицию расчёта');const totals=calcTotals();const rowsSnapshot=state.rows.map(r=>({...r,data:r.data?{...r.data}:{}}));const ctxSnapshot={layout_status:val('calcLayoutStatus'),order_type:val('calcOrderType'),comment:val('calcComment')};const data=await api({action:'create_order',lead_id:state.activeLead?.id||null,project_name:'Заказ '+(val('calcClientName')||val('calcClientPhone')||'РА Лидер'),client_name:val('calcClientName'),client_phone:val('calcClientPhone'),source:val('calcSource'),order_type:val('calcOrderType'),deadline:val('calcDeadline'),layout_status:val('calcLayoutStatus'),comment:val('calcComment'),payment_status:'Не оплачено',rows:state.rows.map(r=>({catalog_id:r.catalog_id||null,category:r.category||null,item_type:r.item_type||r.type,calculation_mode:r.calculation_mode||null,min_client_price:r.min_client_price??null,default_client_price:r.default_client_price??null,markup_percent:r.markup_percent??null,name:`[${r.type}] ${r.name}`,unit:r.unit,qty:r.qty,price:r.price,client_sum:Number(r.client||0)*Number(r.qty||0),contractor_sum:Number(r.price||0)*Number(r.qty||0),comment:r.comment,data:r.data||{}})),totals});if(data.order){toast('Заказ создан');state.rows=[];renderCalcRows();state.activeLead=null;showActiveLead();await loadLeads();if(window.LeaderV2Orders?.load)await window.LeaderV2Orders.load(true).catch(()=>{});openPage('orders');setTimeout(()=>{try{if(window.LeaderV2PostOrderFlow?.show)window.LeaderV2PostOrderFlow.show(data.order,rowsSnapshot,ctxSnapshot)}catch(e){console.warn(e)}},500)}}
function renderDashboard(){const active=state.leads.filter(l=>l.status!=='Спам');if($('statNewLeads'))$('statNewLeads').textContent=active.filter(l=>(l.status||'Новая')==='Новая').length;if($('statWorkLeads'))$('statWorkLeads').textContent=active.filter(l=>(l.status||'')==='В работе').length;if($('statWaitingLeads'))$('statWaitingLeads').textContent=active.filter(l=>['Ждём ответ','КП отправлено','Уточнение деталей'].includes(l.status||'')).length;if($('statConvertedLeads'))$('statConvertedLeads').textContent=active.filter(l=>(l.status||'')==='Создан заказ').length;const list=active.filter(l=>['Новая','В работе','Уточнение деталей','Ждём ответ','КП отправлено'].includes(l.status||'Новая')).slice(0,6);if($('todayList'))$('todayList').innerHTML=list.map(l=>`<div class="work-item"><b>${esc(l.name||'Без имени')}</b> ${statusBadge(l.status||'Новая')}<div class="meta">${esc(l.service||'Услуга не указана')} • ${esc(l.phone||'телефон не указан')}</div></div>`).join('')||'<div class="empty">Активных заявок нет.</div>'}
function openLeadModal(){['leadName','leadPhone','leadService','leadBudget','leadMessage'].forEach(id=>{if($(id))$(id).value=''});if($('leadSource'))$('leadSource').value='MAX';if($('leadCity'))$('leadCity').value='Борисоглебск';$('leadModal')?.classList.remove('hidden')}
function closeLeadModal(){$('leadModal')?.classList.add('hidden')}
function openPage(id){document.querySelectorAll('.page').forEach(p=>p.classList.toggle('active',p.id===id));document.querySelectorAll('.tab').forEach(t=>t.classList.toggle('active',t.dataset.page===id));if(id==='dashboard')renderDashboard();if(id==='leads')renderLeads();if(id==='calc')renderCalcRows();if(id==='orders'&&window.LeaderV2Orders?.load)window.LeaderV2Orders.load(true).catch(()=>{})}
function bind(){ensureResetAuthButton();if($('loginBtn'))$('loginBtn').onclick=login;if($('logoutBtn'))$('logoutBtn').onclick=logout;if($('dashReloadBtn'))$('dashReloadBtn').onclick=()=>loadCrmData().then(()=>toast('Данные обновлены')).catch(e=>alert(e.message));if($('reloadLeadsBtn'))$('reloadLeadsBtn').onclick=()=>loadLeads().then(()=>toast('Заявки обновлены')).catch(e=>alert(e.message));if($('newLeadBtn'))$('newLeadBtn').onclick=openLeadModal;if($('closeLeadModal'))$('closeLeadModal').onclick=closeLeadModal;if($('saveLeadBtn'))$('saveLeadBtn').onclick=()=>createManualLead().catch(e=>alert(e.message));if($('leadStatusFilter'))$('leadStatusFilter').onchange=renderLeads;if($('leadSourceFilter'))$('leadSourceFilter').onchange=renderLeads;if($('leadSearch'))$('leadSearch').oninput=renderLeads;if($('addItemBtn'))$('addItemBtn').onclick=addItem;if($('clearCalcBtn'))$('clearCalcBtn').onclick=()=>{if(confirm('Очистить расчёт?')){state.rows=[];renderCalcRows()}};if($('quoteSentBtn'))$('quoteSentBtn').onclick=()=>markQuoteSent().catch(e=>alert(e.message));if($('createOrderBtn'))$('createOrderBtn').onclick=()=>createOrder().catch(e=>alert(e.message));document.querySelectorAll('.tab').forEach(t=>t.onclick=()=>openPage(t.dataset.page));if($('leadList'))$('leadList').onclick=async(e)=>{const card=e.target.closest('.lead-card');if(!card)return;const lead=state.leads.find(l=>l.id===card.dataset.id);if(!lead)return;const action=e.target.dataset.action;try{if(action==='calc')leadToCalc(lead);if(action==='client')await ensureClient(lead);if(action==='spam')await updateLead(lead.id,{status:'Спам'})}catch(err){alert(err.message)}};if($('leadList'))$('leadList').onchange=async(e)=>{const card=e.target.closest('.lead-card');if(!card||e.target.dataset.action!=='status')return;try{await updateLead(card.dataset.id,{status:e.target.value});toast('Статус обновлён')}catch(err){alert(err.message)}};if($('calcRows'))$('calcRows').onclick=(e)=>{const i=e.target.dataset.remove;if(i!==undefined){state.rows.splice(Number(i),1);renderCalcRows()}}}
async function boot(){bind();renderCalcRows();renderDashboard();renderLeads();const ok=await checkAuth();if(ok){try{await loadCrmData()}catch(e){toast('Не удалось загрузить CRM: '+e.message)}}}
document.addEventListener('DOMContentLoaded',boot);
